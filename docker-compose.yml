services:
  backend:
    build: ./backend
    container_name: backend
    restart: unless-stopped

  frontend:
    build: ./frontend
    mem_limit: 4g
    memswap_limit: 4g
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend